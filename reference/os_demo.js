const os = require('os');

// Plateform
console.log(os.platform());

// CPU Arch
console.log(os.arch());

// CPU Core Info
console.log(os.cpus())

// Free memory
console.log(os.freemem());

// Total memory
console.log(os.totalmem());